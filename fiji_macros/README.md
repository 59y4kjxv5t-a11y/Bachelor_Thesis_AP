# Fiji Macros

Macros for preprocessing raw z-image stacks before pixel classification.

- Z-projection (Maximum Intensity) + Channel merging (GFP, cFos)
- Post Ilastik: invert and normalise pixel values of segmentation TIFFs + rename files
